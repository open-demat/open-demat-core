# config/routes.yaml

# (import d’un dossier de routes additionnelles)
extra_routes:
  resource: 'routes/*.yaml'

kernel:
  resource: ../src/Kernel.php
  type: attribute

# importe aussi les contrôleurs à attributs du core si tu en as
core_controllers:
  resource: '../src/Controller/'
  type: attribute

# config/routes.yaml
per_bundles_auto:
  resource: .
  type: per_bundle_attributes