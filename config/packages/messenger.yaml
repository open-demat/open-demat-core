framework:
  messenger:
    transports:
      async: '%env(MESSENGER_TRANSPORT_DSN)%'

    routing:
      Symfony\Component\Mailer\Messenger\SendEmailMessage: async
      OpenDemat\Core\Mailer\Message\TemplatedMailMessage: async
      OpenDemat\Core\EsupSignature\Messenger\Message\ProcessBpmSignalMessage: async
